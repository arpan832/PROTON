# Slideshow images

Put your slideshow images in this folder, then update the `SLIDES` array in `popup/popup.js` with their filenames.

Example:

```js
const SLIDES = [
  'popup/images/spidey-1.jpg',
  'popup/images/spidey-2.png'
];
```
